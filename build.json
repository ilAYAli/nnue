{
  "description": "Reckless lane: scale the check-bucket-mask2 existing-weight delta before broad gates.",
  "metadata": {
    "experiment": "reckless-checkbucket-mask2-delta-scale",
    "track": "nnue_reckless",
    "hypothesis": "The full check-bucket-mask2 delta had useful narrow/non-mate signal but broad mate-like tails. Smaller exported deltas may preserve useful move-choice changes while avoiding tail blowups.",
    "base_net": "runs/reckless-check-bucket-mask2-lr1e6-e4/base_bucketed.nn",
    "candidate_net": "runs/reckless-check-bucket-mask2-lr1e6-e4/model.nn",
    "scales": [0.025, 0.05, 0.075, 0.10, 0.15, 0.20, 0.25, 0.35],
    "no_sprt": true
  },
  "validation": {
    "tmux": "nnue_reckless",
    "required_checks": [
      "generate scaled .nn files with tools/nnue/scale_net_delta.py",
      "net-diff each scaled net against the bucketed base",
      "broad search-target gate at 300k nodes",
      "no SPRT unless all, non-mate, and mate-like splits are clean"
    ]
  }
}
