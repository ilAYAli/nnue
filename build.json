{
  "description": "Active Enyo NNUE improvement-run config. This file is the reviewed source of truth for the current candidate recipe.",
  "experiment": "Native Enyo-owned Bullet scratch full-data run",
  "goal": "Train a clean Enyo-native .nn from scratch with Bullet using Enyo's current runtime layout. This is the nnue_native lane: no Berserk initialization; validate by static/composite gates before any SPRT.",
  "track": "nnue_native",
  "inputs": {
    "broad": "runs/imported/fresh_d12self18h64_d16_labels_20260519_113826/score/labeled.jsonl"
  },
  "create": {
    "name": "native-bullet-enyo-scratch-full-eval400-sb16384",
    "backend": "bullet",
    "labeled_jsonl": "runs/imported/fresh_d12self18h64_d16_labels_20260519_113826/score/labeled.jsonl",
    "bullet_mode": "enyo",
    "init_net": "",
    "bullet_rows": 3000000,
    "bullet_hidden": 1024,
    "bullet_l2": 16,
    "bullet_batch_size": 4096,
    "bullet_batches": 64,
    "bullet_superbatches": 16384,
    "bullet_threads": 4,
    "bullet_wdl": 0.75,
    "bullet_lr": 0.001,
    "bullet_final_lr": 5e-05,
    "bullet_eval_scale": 400.0,
    "bullet_save_rate": 4096,
    "event_command": "$HOME/code/cpp/chess/nnue/tools/events/nnue_event_ntfy.sh"
  },
  "validation": {
    "status": "training",
    "success_condition": [
      "exported model.nn loads in Enyo and validate.py",
      "static metrics improve meaningfully over prior native scratch attempts",
      "composite move-choice gate is not clearly worse than reference",
      "mate-like/tactical-tail slice does not collapse",
      "no SPRT unless the cheap gates justify match time"
    ],
    "next_after_success": [
      "run static validation on the imported d12/d16 pack",
      "run composite move-choice gate against the current reference",
      "only run SPRT if gates are clean"
    ]
  }
}
