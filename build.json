{
  "description": "Train a held-out sidecar policy-ranker diagnostic on the 74 search-descendant targets without changing scalar NNUE eval.",
  "experiment": "policy-desc74-board-h128-t0-val25-r14-lr2e4-e2000",
  "create": {
    "name": "policy-desc74-board-h128-t0-val25-r14-lr2e4-e2000",
    "backend": "policy-ranking",
    "init_net": "runs/child-ranking-fast4-r14-rank2-listwise-qfwd-refpreserve20-dz5-lr5e5-e360/train/child-ranking-fast4-r14-rank2-listwise-qfwd-refpreserve20-dz5-lr5e5-e360/model.nn",
    "policy_targets": "targets/search-descendant-r20-smokeworse29-20260529/search_descendant_child_targets.jsonl",
    "policy_hidden": 128,
    "policy_feature_set": "board",
    "policy_dropout": 0.0,
    "policy_val_fraction": 0.25,
    "policy_target_temperature_cp": 35,
    "policy_thresholds": "0",
    "policy_export_threshold": 0,
    "policy_gate_min_top1": 55,
    "policy_gate_max_bad": -1,
    "policy_gate_min_val_top1": 12,
    "policy_gate_max_val_bad": 0,
    "policy_gate_min_val_good": 3,
    "policy_bad_tolerance_cp": 10,
    "lr": 0.0002,
    "epochs": 2000,
    "rank_temperature_cp": 1,
    "min_groups": 74,
    "device": "cpu",
    "weight_decay": 0.000001,
    "event_command": "$HOME/code/cpp/chess/nnue/tools/events/nnue_event_ntfy.sh"
  },
  "validation": {
    "baseline": [
      "r14 scalar child eval on the 74 search-descendant groups: 34/74",
      "preserved scalar child-ranking lr2e-5: .pt/.nn 35/74",
      "preserved scalar child-ranking lr5e-5: .pt/.nn 36/74",
      "target-only scalar child-ranking lr5e-5: .pt/.nn and engine 61/74, but broad static collapsed",
      "in-sample policy sidecar reached 74/74 with 40 good overrides and 0 bad"
    ],
    "pass_intent": [
      "test whether the non-scalar sidecar generalizes beyond the rows it trains on",
      "require held-out selected policy top1 at threshold 0 to reach at least 12/18",
      "require at least 3 held-out good overrides and 0 held-out bad overrides"
    ]
  }
}
