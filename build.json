{
  "description": "Active Enyo NNUE improvement-run config. Use this as the reviewed candidate recipe.",
  "experiment": "pairwise-broadtail-reference-diagnostic",
  "goal": "Broaden the pairwise target set after qmid3 improved the narrow SPRT-failure gate but failed repeated-tail and replay failure-suite gates. Start from the current reference net, not a damaged qmid checkpoint.",
  "inputs": {
    "broad": "~/code/cpp/chess/nnue/runs/imported/fresh_d12self18h64_d16_labels_20260519_113826/score/labeled.jsonl",
    "pairwise_scores": "~/code/cpp/chess/nnue/assets/failure_suite/pairwise_sprtfail_repeated_tail_scores_20260522.csv"
  },
  "create": {
    "name": "pairwise-broadtail-ref-w8-lr5e4-e16",
    "backend": "pairwise",
    "labeled_jsonl": "~/code/cpp/chess/nnue/runs/imported/fresh_d12self18h64_d16_labels_20260519_113826/score/labeled.jsonl",
    "pairwise_scores_csv": "~/code/cpp/chess/nnue/assets/failure_suite/pairwise_sprtfail_repeated_tail_scores_20260522.csv",
    "pairwise_pair_weight": 8.0,
    "pairwise_pair_batch_size": 64,
    "pairwise_pair_beta": 100,
    "pairwise_min_target_margin": 20,
    "pairwise_max_target_margin": 600,
    "pairwise_loss_weight_by_cp": true,
    "objective": "huber",
    "target_clamp": 800,
    "huber_beta": 200,
    "lr": 0.0005,
    "epochs": 16,
    "batch_size": 8192,
    "max_rows": 100000,
    "pack_limit": 100000,
    "device": "cuda",
    "workers": 2,
    "event_command": "$HOME/code/cpp/chess/nnue/tools/events/nnue_event_ntfy.sh",
    "forward": "quantized",
    "init_net": "~/code/cpp/chess/enyo/nnue/berserk-d43206fe90e4.nn"
  },
  "validation": {
    "status": "broad-tail pairwise diagnostic from current reference",
    "success_condition": [
      "net_diff shows controlled exported movement",
      "static validation is not obviously worse than recent qmid candidates",
      "repeated-tail move-choice gate does not regress",
      "failure-suite replay gate is non-negative with controlled tail",
      "no SPRT unless broad gates become clean"
    ],
    "next_after_success": [
      "run net_diff against current reference",
      "run repeated-tail move-choice gate",
      "run replay failure-suite gate",
      "only then consider a 1000-game smoke SPRT"
    ]
  }
}
