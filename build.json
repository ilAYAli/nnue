{
  "description": "Active Enyo NNUE improvement-run config. This file is the reviewed source of truth for the current candidate recipe.",
  "experiment": "Native Bullet-Enyo scratch eval-scale calibration",
  "goal": "Retest the native Enyo-owned scratch lane with the same Bullet-Enyo architecture and data, but lower eval scale after eval400 over-amplified outputs and failed static/composite gates.",
  "track": "nnue_native",
  "engine_branch": "none",
  "nnue_branch": "feature/nnue-native-bullet-eval200",
  "inputs": {
    "broad": "runs/imported/fresh_d12self18h64_d16_labels_20260519_113826/score/labeled.jsonl",
    "init_net": ""
  },
  "create": {
    "name": "native-bullet-enyo-scratch-5m-eval200-sb4096",
    "backend": "bullet",
    "bullet_mode": "enyo",
    "labeled_jsonl": "runs/imported/fresh_d12self18h64_d16_labels_20260519_113826/score/labeled.jsonl",
    "init_net": "",
    "trainable": "all",
    "weight_decay": 1e-06,
    "bullet_rows": 5000000,
    "bullet_hidden": 1024,
    "bullet_l2": 16,
    "bullet_batch_size": 4096,
    "bullet_batches": 64,
    "bullet_superbatches": 4096,
    "bullet_threads": 4,
    "bullet_wdl": 0.0,
    "bullet_lr": 0.001,
    "bullet_final_lr": 0.0001,
    "bullet_enyo_l0_std": 8.0,
    "bullet_enyo_l1_std": 1.0,
    "bullet_enyo_l1_export_scale": 1.0,
    "bullet_eval_scale": 200.0,
    "bullet_save_rate": 1024,
    "event_command": "$HOME/code/cpp/chess/nnue/tools/events/nnue_event_ntfy.sh"
  },
  "validation": {
    "status": "pending",
    "required_before_sprt": [
      "static validation must be materially closer to reference than eval400 native scratch",
      "composite move-choice gate must be much better than previous native scratch runs",
      "SPRT only if static/composite gates show a credible near-reference candidate"
    ]
  }
}
