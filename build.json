{
  "description": "No active training run. The previous native one-bucket SF-binpack scale-up is rejected; replace this config with a concrete architecture experiment before running build.py create.",
  "metadata": {
    "experiment": "native-lowbucket-wide-architecture-audit",
    "track": "nnue_native",
    "status": "paused",
    "last_rejected": "native-bullet-enyo1-sfbinpack-long-eval400-lr1e3-sb8192",
    "reason": "checkpoint 1024 failed the 800-target gate: all 71 vs 531, capped -73291, worst -32000"
  },
  "create": {
    "disabled": true,
    "name": "native-lowbucket-wide-architecture-audit",
    "run_dir": "runs/native-lowbucket-wide-architecture-audit"
  },
  "next_hypothesis": {
    "lane": "nnue_native",
    "intent": "try a low-input-bucket wider native architecture only after trainer, loader, evaluator, parity, and NPS support are explicit",
    "constraints": [
      "no Berserk initialization",
      "Bullet/binpack training path",
      "no runtime dispatch overhead for the selected net",
      "no training before build.json names the concrete architecture and gates"
    ],
    "prerequisite_checks": [
      "trainer/export supports the intended hidden width and bucket count",
      "engine loader/evaluator accepts the exact .nn layout",
      "init/export/roundtrip parity passes",
      "NPS regression is measured before training"
    ]
  },
  "validation": {
    "gate": "parity and NPS first; then short Bullet/binpack smoke; then 800-target broad gate",
    "promotion": "not eligible for SPRT until search gate and failure replay are clean"
  }
}
