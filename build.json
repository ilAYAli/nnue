{
  "description": "Active Enyo NNUE improvement-run config. Use this as the reviewed candidate recipe.",
  "experiment": "scratch-export-scale-check",
  "goal": "Test a scratch initializer whose scale survives Enyo's raw integer .nn export.",
  "create": {
    "name": "scratch-berserkish-100k-lr1e4-e10",
    "backend": "pytorch",
    "labeled_jsonl": "~/code/cpp/chess/nnue/runs/imported/fresh_d12self18h64_d16_labels_20260519_113826/score/labeled.jsonl",
    "pack_limit": 120000,
    "init_net": null,
    "init": "berserk-ish",
    "objective": "huber",
    "target_clamp": 800,
    "huber_beta": 200,
    "lr": 0.0001,
    "epochs": 10,
    "batch_size": 4096,
    "device": "cuda",
    "workers": 2,
    "max_rows": 100000,
    "val_rows": 20000,
    "grad_norm_every": 20,
    "select_metric": "sign",
    "patience": 0,
    "event_command": "$HOME/code/cpp/chess/nnue/tools/events/nnue_event_ntfy.sh"
  },
  "validation": {
    "status": "export-scale preflight",
    "success_condition": [
      "training loss decreases across epochs",
      "gradient norms are nonzero for sparse input, L1, L2, and output layers",
      "exported model.nn is non-empty and can be evaluated by static validation"
    ],
    "next_after_success": [
      "compare float .pt validation against exported .nn validation",
      "increase LR or add fake/export-aware quantization if export is still destructive",
      "only scale rows after exported .nn roughly matches float .pt behavior"
    ]
  }
}
