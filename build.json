{
  "run": "native-next-explicit-example",
  "description": "Template for the simple NNUE runner. Copy and edit before launch.",
  "hypothesis": "State exactly why this run should improve the current native reference.",
  "changed_variables": {
    "base_net": "runs/current-reference/train/model.nn",
    "data": "primary:uho52:sf:berserk",
    "objective": "output-only",
    "lr": "1e-10",
    "batch": 512,
    "forge": "../crucible/scripts/forge",
    "engine_hash": "replace-engine-hash",
    "candidate_nnue": "{run_dir}/train/model.nn",
    "reference_nnue": "default.net",
    "smoke_games": 300,
    "sprt_games": 1500
  },
  "run_dir": "runs/{run}",
  "stages": [
    {
      "name": "train",
      "why": "Train only the variables listed in changed_variables.",
      "command": [
        "echo",
        "replace ./nnue-train command for {run}"
      ]
    },
    {
      "name": "static_gate",
      "why": "Reject if held-out metrics do not improve without broad drift.",
      "command": [
        "echo",
        "replace static gate command for {run}"
      ]
    },
    {
      "name": "smoke_if_candidate",
      "why": "Launch a small Crucible smoke through forge only when static_gate marks the net candidate-worthy.",
      "command": [
        "{var_forge}",
        "nnue",
        "{var_engine_hash}",
        "--candidate-nnue",
        "{var_candidate_nnue}",
        "--reference-nnue",
        "{var_reference_nnue}",
        "--",
        "--games",
        "{var_smoke_games}"
      ]
    },
    {
      "name": "sprt_if_smoke_passes",
      "why": "Launch the larger Crucible SPRT only after the smoke is neutral-positive.",
      "command": [
        "{var_forge}",
        "nnue",
        "{var_engine_hash}",
        "--candidate-nnue",
        "{var_candidate_nnue}",
        "--reference-nnue",
        "{var_reference_nnue}",
        "--",
        "--games",
        "{var_sprt_games}"
      ]
    }
  ],
  "gates": [
    "train/export command must produce one model.nn",
    "static gate must report changed-variable metrics and decision",
    "smoke is allowed only for candidate_for_smoke_review",
    "SPRT is allowed only after smoke is neutral-positive"
  ]
}
