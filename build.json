{
  "run": "native-10.0.0-rc1",

  "architecture": "architecture.json",
  "architecture_overrides": {
    "output_buckets": 8
  },
  "hypothesis": "Keep the 1-input-bucket Enyo-native feature layout that now has clean export/eval parity, but split the output head into 8 material-count buckets. This tests whether the simple lane failed partly because one shared output head was underfitting across material phases.",
  "changed_variables": {
    "architecture": "Set architecture_overrides.output_buckets=8; keep input_buckets=1, feature_channels=12, hidden=1024, data, dose, WDL, LR, and l0_std=32.0 unchanged from native-9.2.0-rc1."
  },
  "training": {
    "l0_std": 32.0
  },
  "notes": [
    "native-9.2.0-rc1 passed Python .nn vs Enyo evalnet parity within 2 cp, so this is not an export/layout rescue run.",
    "This is a new native major lineage because output-bucket runtime semantics change.",
    "Reject immediately if load/eval parity fails or if the 100-game smoke is negative."
  ]
}
