{
  "description": "No active training run. The true 8-runtime-bucket native smoke is rejected; replace this config with a concrete new data-scale or architecture hypothesis before running build.py create.",
  "metadata": {
    "experiment": "native-bullet-enyo8-runtime-sfbinpack-smoke",
    "track": "nnue_native",
    "status": "rejected",
    "hypothesis": "A true 8-king-bucket runtime net may reduce bucket starvation and improve early native scratch behavior compared with 16/32-bucket layouts. The engine is compiled for 8 buckets, so the exported net is not expanded at load time.",
    "baseline": "native-bullet-enyo16-sfbinpack-smoke-eval400-lr1e3-sb2048",
    "reason": "best checkpoint 1536 failed the 800-target gate: all 82 vs 277, capped -19627, worst -32000"
  },
  "create": {
    "disabled": true,
    "name": "native-bullet-enyo8-runtime-sfbinpack-smoke-eval400-lr1e3-sb2048",
    "run_dir": "runs/native-bullet-enyo8-runtime-sfbinpack-smoke-eval400-lr1e3-sb2048"
  },
  "validation": {
    "gate": "800-target Lichess-policy checkpoint sweep",
    "promotion": "not eligible for SPRT"
  },
  "next_hypothesis": {
    "lane": "nnue_native",
    "intent": "stop same-data scalar bucket-count and hidden-width smoke runs; next work needs either materially larger/better training data or a new architecture objective with a measurable reason to improve search move choice",
    "constraints": [
      "no Berserk initialization for native runs",
      "Bullet/binpack training path",
      "no runtime dispatch overhead for the selected net",
      "no training before build.json names the concrete hypothesis and gate"
    ]
  }
}
